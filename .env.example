# ── Cloudflare credentials ─────────────────────────────────────────────────
# Get from: https://dash.cloudflare.com/profile/api-tokens
# Token needs "Edit Cloudflare Workers" and "Edit Worker Routes" permissions.
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here

# ── Hyperdrive ─────────────────────────────────────────────────────────────
# After running: wrangler hyperdrive create coderclawlink-db --connection-string="postgres://..."
# Paste the returned ID into api/wrangler.toml  [[hyperdrive]] id = "..."
# (Not needed in .env — stored in wrangler.toml)

# ── JWT Secret ────────────────────────────────────────────────────────────────────
# Set via wrangler secret (not stored in .env for production):
#   wrangler secret put JWT_SECRET
# For local dev, add to a .dev.vars file at api/.dev.vars:
JWT_SECRET=replace_with_a_long_random_string_min_32_chars

# ── Database (for running migrations locally) ─────────────────────────────────
DATABASE_URL=postgres://user:password@host:5432/dbname
